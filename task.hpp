#ifndef TASK
#define TASK

#include "lib.hpp"

using std::pair;
using std::vector;
using std::string;

extern const vector<pair<string, string>> test_data;
void solve(std::istream& in, std::ostream& out);

#endif
